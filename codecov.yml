ignore:
  - "sdex_group/_version.py"
  - "setup.py"
  - "versioneer.py"

coverage:
  status:
    project:
      default:
        target: auto
        paths:
          - "sdex_group"
        informational: true
    patch:
      default:
        target: auto
        paths:
          - "sdex_group"
        informational: true

